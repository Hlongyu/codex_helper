# Skill management uses full local scan

Skill management refreshes by scanning all configured Skill Locations for known local Agent Clients, using the first-version rule of direct child directories containing `SKILL.md`. A full local scan lets XXSwitch present Client Skills, Shared Skills, exposures, and conflicts consistently instead of discovering them only when a specific Agent Client is opened.
