# Skill management is local-only

The first version of Skill management only coordinates Agent Clients and Skill Locations on the local machine. Cross-machine synchronization is out of scope because it introduces remote path mapping, OS-specific link behavior, permissions, latency, and conflict semantics that are separate from the local sharing problem.
