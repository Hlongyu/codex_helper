# Skill management only supports known Agent Clients

Skill management is limited to the known Agent Clients that Codex Helper explicitly supports. Users can configure Skill Locations for those supported clients, but cannot add arbitrary custom Agent Client types; this keeps exposure behavior tied to known local discovery mechanisms instead of turning Codex Helper into a generic filesystem synchronizer.
