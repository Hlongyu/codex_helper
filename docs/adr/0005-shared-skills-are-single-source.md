# Shared Skills are single-source through symlink exposure

Shared Skills are modeled as a single canonical Skill in the Skill Library, with Agent Clients receiving directory symlink Skill Exposures to that canonical content. The first version does not generate copied derivatives, wrappers, or manifest adapters; Agent Clients that cannot consume a symlink exposure are not supported by Skill sharing until a later explicit adapter decision is made.
